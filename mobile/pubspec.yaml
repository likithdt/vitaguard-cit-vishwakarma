name: vitalguard
description: VitalGuard - Real-Time Health Monitoring Phase 1 + Phase 2
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  web_socket_channel: ^2.4.5
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
